# Rules and Conventions

Repo-wide conventions that aren't captured elsewhere (`AGENTS.md` for
executable working rules, ADRs in `docs/decisions/` for binding
architecture decisions).

- **PR size:** keep the changed-file count of a pull request below 15
  files. If a change doesn't fit, split it into stacked PRs rather than
  landing it as one large PR.
